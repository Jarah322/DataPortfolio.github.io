import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Data Science Portfolio Service | Professional Portfolio Websites',
  description: 'Get a custom data science portfolio website that showcases your skills and projects. Fast turnaround, industry-tailored designs, and proven career impact.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
} 